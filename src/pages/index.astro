---
import OpenGraph from '@components/OpenGraph.astro'
import Slider from '@components/ui/Slider.astro'
import Social from '@components/ui/Social.astro'
import Layout from '@layouts/Layout.astro'

import BentoGrid from './_index/BentoGridSection.astro'
import Countdown from './_index/CountdownSection.astro'
import Hero from './_index/HeroSection.astro'

const options = {
  title: 'JSConf Chile 2025 | La más prestigiosa conferencia de JavaScript',
  description:
    'Acompáñanos en la tercera edición de la JSConf Chile donde tendremos Charlistas internacionales, comunidad, aprendizaje y conexiones.'
}
---

<Layout options={options}>
  <OpenGraph
    description={options.description}
    image="/og/og.jpg"
    slot="social-media"
    title={options.title}
  />

  <main>
    <Hero />
    <Countdown />
    <BentoGrid />
    <Slider />

    <section style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', minHeight: '100vh' }}>Aqui va el card horizontal</section>

    <Social />
  </main>
</Layout>
